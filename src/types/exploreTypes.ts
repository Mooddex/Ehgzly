import { LucideIcon } from "lucide-react";

export interface AIStarter {
  title: string;
  href: string;
  icon: LucideIcon;
  description: string;
  gradient: string;
  bgColor: string;
  borderColor: string;
};
export interface MousePosition {
  x: number;
  y: number;
}